# ACT 수집·학습 통합 계획 — 2026-08-22

| | |
|---|---|
| 작성 | 2026-08-22 |
| 전제 변경 | **수집과 학습을 모두 이 노트북(RTX 4060)에서 진행한다.** `ACT_수집_다른컴퓨터_가이드.md`의 "수집은 별도 컴퓨터, 학습은 원 노트북" 분리 전제는 **폐기** |
| 근거 | `ACT_수집_작업기록_2026-08-24.md` — 다른 컴퓨터(사용자 `rkdgm`)에서 수집 시도, **유효 에피소드 0개로 종료**. 원인 미진단 상태로 작업 중단 |
| 마감 | 2026-09-04 (D-13) |

---

## 0. 결론 요약

- 8/22 시도는 **실패**했다. 텔레옵 연결 로그(`SOLeader connected` 등)는 떴지만 **팔로워암이 실제로 움직이지 않았다** — 연결 성공 ≠ 텔레옵 성공. 원인 진단 전이므로 재발 가능성을 배제할 수 없다.
- 8/22에 만든 도구(수집표 생성기, 데이터셋 검사기, 실행 스크립트)는 **설계 자체는 재사용 가치가 있다.** 다만 그 컴퓨터의 lerobot 버전(0.6.1)이 이 노트북(0.4.4)과 다르므로 **그대로 복사해도 바로 안 돌 수 있다.**
- 이 노트북은 8/21 시점에 **팔·카메라 텔레옵이 60Hz·무오류 30초로 이미 검증된 상태**였다 (`로봇암_구축기록.md` §5-7 이력, `calibration/follower_home.json` 최신값 보유). 8/22 실패의 원인이 하드웨어 자체 결함인지, 그 컴퓨터·배선 쪽 문제인지는 **이 노트북에서 재검증해야 확실해진다.**
- 사용자 결정: 8/22 도구는 **그 컴퓨터에서 직접 복사**해 가져온다 (git pull이나 재작성이 아님).

---

## 1. 이 노트북 현재 상태 (2026-08-22 점검 결과)

| 항목 | 값 |
|---|---|
| GPU | NVIDIA RTX 4060, CUDA 13.1 드라이버, `torch.cuda.is_available() == True` |
| conda 환경 | `C:\Users\sehi5\miniconda3\envs\lerobot`, Python 3.10.20 |
| torch | `2.10.0+cu128` (CUDA 빌드 — 학습용으로 이미 맞는 상태) |
| lerobot | **0.4.4** 🔴 8/22 그 컴퓨터는 **0.6.1** — 버전 불일치 |
| git | 이 폴더는 git 저장소가 **아니다** (`.git` 없음). 8/22 커밋 이력(`74622a4` 등)은 이 노트북에 없다 |
| 팔로워 포트 | COM3 (`teleop.ps1` 기본값) |
| 리더 포트 | COM5 |
| 손목 카메라 인덱스 | 0 (`teleop.ps1` 기본값 — 8/22 그 컴퓨터는 1이었다. **인덱스는 기기마다 다르므로 재확인 필수**) |
| `calibration/follower_home.json` | 8/21 재조정한 최신값 그대로 존재 (shoulder_pan 2459 / shoulder_lift 1479 / elbow_flex 3015 / wrist_flex 2567 / wrist_roll 1032 / gripper 1329) |
| `output/` 데이터셋 폴더 | 없음 — 8/22의 빈 `act_red_yellow_v1` 잔재도 없음. **깨끗한 상태에서 시작 가능** |
| ACT 수집 전용 도구 | **없음** — `tools/act_collection_plan.py`, `tools/check_act_dataset.py`, `record_act.ps1`, `docs/superpowers/**`, `tests/act_collection/**` 전부 미존재 |

---

## 2. 버전 불일치(0.4.4 vs 0.6.1)가 실제로 영향을 주는 지점

8/22 작업기록 §7.1에 이미 한 번 걸렸던 사례가 있다 — `--dataset.display_data=true`(0.6.1 문법)가 실제로는 최상위 옵션 `--display_data=true`여야 했다. **이런 CLI 플래그 이름·위치 차이가 버전마다 있다.** 그대로 복사한 `record_act.ps1`을 이 노트북에서 돌리면 같은 종류의 오류가 다시 날 가능성이 높다.

**대응**: lerobot을 0.6.1로 올리지 않는다 (이 노트북은 이미 검증된 0.4.4 + CUDA 조합이라 마감 10일 전에 버전을 흔드는 리스크가 더 크다). 대신:

```powershell
$py = "C:\Users\sehi5\miniconda3\envs\lerobot\python.exe"
& $py -m lerobot.scripts.lerobot_record --help
```

로 0.4.4의 실제 `lerobot_record` 플래그 목록을 먼저 뽑아, `record_act.ps1` 안의 `--dataset.*` / `--display_data` 류 인자를 이 노트북 버전에 맞게 하나씩 대조·수정한다.

---

## 3. 파일 이관 — 그 컴퓨터에서 복사해올 것

### 3.1 그대로 가져와서 붙여넣기 (이 노트북엔 없는 파일)

| 파일 | 비고 |
|---|---|
| `tools/act_collection_plan.py` | 시드 20260816 고정 100개 수집표 생성기. 버전 의존 없음 — 그대로 사용 가능할 가능성 높음 |
| `tools/check_act_dataset.py` | 데이터셋 완결성 검사기. `meta/info.json` 구조를 읽는 로직이라 lerobot 버전 영향 적음 |
| `tools/run_lerobot_module.py` | Codex 샌드박스의 PATH 문제 보완용 — **이 노트북(일반 PowerShell)에서는 불필요할 가능성 높다.** 가져오되 당장 쓸 필요는 없음 |
| `docs/superpowers/specs/2026-08-24-act-red-yellow-collection-design.md` | 설계 원본 — §5에 이미 핵심 값은 요약해뒀지만 원본을 갖고 있는 게 낫다 |
| `docs/superpowers/plans/2026-08-24-act-red-yellow-collection-implementation.md` | 구현 계획 원본 |
| `tests/act_collection/test_act_collection_plan.py` | |
| `tests/act_collection/test_check_act_dataset.py` | |
| `tests/act_collection/test_act_scripts.py` | (미커밋 상태였음 — 있으면 같이) |

### 3.2 🔴 겹치는 파일 — 절대 덮어쓰기 금지, diff 후 병합만

이 노트북에 **이미 이 하드웨어로 검증된 버전**이 있다. 그 컴퓨터 것으로 그냥 덮으면 8/21에 확인한 COM3/COM5, 인덱스 0 설정과 안전장치가 날아간다.

| 파일 | 이 노트북 상태 | 처리 |
|---|---|---|
| `teleop.ps1` | 8/21 완주 검증됨 (COM3/COM5, 인덱스 0, 4단계 안전검사) | **가져오지 않는다.** 그 컴퓨터의 `record_act.ps1`이 참고한 패턴만 확인용으로 본다 |
| `tools/check_cameras.py` | 8/20 버전 | 그 컴퓨터 버전은 "인덱스 1개 이상 허용"으로 수정됐다 (기존엔 2개 강제). **이 수정 내용만 이 노트북 파일에 반영** — 통째 교체 아님 |
| `record_act.ps1` | 없음 | 새로 가져오되, COM 포트·카메라 인덱스·`--display_data` 등 플래그를 **이 노트북 값으로 다시 설정**해야 한다 (§2 참고) |

### 3.3 가져올 필요 없음

- `output/act_red_yellow_v1/meta/info.json` — 에피소드 0개짜리 빈 메타데이터. 학습 데이터 아님, 이관 무의미

---

## 4. 확정된 수집 설계 (재확인용 — 원본은 §3.1의 design.md)

- 정본 시나리오: 2026-08-09 Claude Artifact (`ACT_수집_작업기록_2026-08-24.md` §3의 링크)
- 정책: 적색·황색 물체 하나의 ACT 정책
- 고정 작업 문장: `Pick up the hazardous object, place it in the isolation bin, and close the lid.`
- 목표 100 에피소드 = 적/황 각 50 × 5위치(중앙/앞좌/앞우/뒤좌/뒤우) × 각 10개, 10배치×10에피소드
- FPS 30 (8/22 실측은 26.5~26.6 — 이 노트북에서 재실측 필요), 손목 카메라 640×480, `observation.images.wrist`
- 에피소드 최대 35초, 리셋 최대 60초
- `push_to_hub=false`
- 데이터셋: `local/hazardbot_red_yellow_act_v1`, 경로 `output/act_red_yellow_v1`

성공/실패 판정 기준과 배치 1의 확정 순서(10개 항목)는 `ACT_수집_작업기록_2026-08-24.md` §4·§9에 이미 확정돼 있다. 다시 정하지 않는다.

---

## 5. 하드웨어 진단 — 수집 재개 전 필수 (이 노트북 기준)

8/22 작업기록 §14의 진단 순서를 이 노트북 값(COM3/COM5, 인덱스는 재확인)으로 그대로 적용한다. **연결 로그만 보고 건너뛰지 말 것 — 8/22 실패의 핵심 교훈이다.**

1. 팔로워암 전원 꺼진 상태로 시작
2. 남아있는 이전 파이썬 프로세스 없는지 확인 (`Get-Process python -ErrorAction SilentlyContinue`)
3. 장치관리자에서 COM3/COM5 그대로인지 확인
4. 팔 주변 비우기
5. `tools/check_home.py --port COM3` — 읽기 전용 확인
6. `tools/check_align.py --leader COM5 --follower COM3`
7. `tools/check_goal.py --port COM3 --fix`
8. 데이터 저장 없이 `teleop.ps1` 짧게 실행
9. **팔로워암이 리더암 6축을 실제로 따라 움직이는지 사람이 눈으로 확인** — 8/22는 이 단계를 건너뛴 게 아니라 여기서 실패를 확인하고도 본 수집으로 넘어갔던 게 문제였다
10. 정상 종료 후 토크 해제 확인
11. 그 다음에만 pilot 1개 → 배치 1

---

## 6. 전체 작업 순서

1. **[사용자]** 그 컴퓨터에서 §3.1 파일 + §3.2의 `check_cameras.py` diff를 이 노트북으로 복사
2. `lerobot_record --help`로 0.4.4 실제 플래그 확인 → `record_act.ps1`을 이 노트북 버전에 맞게 수정
3. `tools/check_cameras.py --list`로 이 노트북에서의 손목 카메라 인덱스 재확인 (0 가정하지 말 것)
4. §5 하드웨어 진단 1~10번 전부 통과 확인
5. `record_act.ps1 -Pilot` (또는 동등 절차)로 **pilot 에피소드 1개** 성공 — 영상·관절·액션·프레임 수 전체 검사
6. pilot 통과 시에만 `record_act.ps1 -Batch 1`부터 순서대로 10배치 진행
7. 100개 완료 후 `tools/check_act_dataset.py --require-complete`로 최종 검증
8. 이 노트북에서 바로 `lerobot-train` 실행 (컴퓨터 이관 불필요 — 이미 CUDA 확인됨)

---

## 6.5 🔴 실제 수집 중 발견 — 데이터셋 경로에 한글이 있으면 안 됨

배치 1 진행 중 두 번째 크래시에서 발견: `output\act_red_yellow_v1`(=`OneDrive\바탕 화면\HazardBot\...`) 경로에 저장하면 **에피소드가 누적돼 비디오 청크 파일이 새로 생성될 때(청크 경계, 대략 몇 개 에피소드마다) ffmpeg concat이 실패**한다. 원인은 `바탕 화면`(한글) 경로를 ffmpeg의 concat 임시 파일이 못 읽는 것 — 콘솔 표시 문제가 아니라 실제 파일 I/O 실패다.

**조치 완료**: 데이터셋 저장 위치를 `C:\ACT_data`(한글 없음, OneDrive 밖)로 이전. `record_act.ps1`의 `-DatasetRoot` 기본값도 이쪽으로 변경 완료 — 앞으로는 플래그 없이 실행해도 자동으로 안전한 경로에 저장된다.

- 새 위치: `C:\ACT_data\act_red_yellow_v1`, `C:\ACT_data\act_red_yellow_v1_pilot`
- `output\act_red_yellow_v1`(구 위치, OneDrive 안)는 **폐기 — 더 이상 쓰지 않는다.** 에피소드 0·1은 새 위치로 이미 복사·검증 완료
- 스크립트 파일들(`tools/*.py`, `record_act.ps1` 등)은 그대로 저장소(OneDrive 경로)에 둬도 무방 — 문제는 ffmpeg가 쓰는 **비디오 데이터 경로**에서만 발생

## 6.8 🔴 학습 중 데이터 무결성 버그 발견·수정 (2026-08-24)

`lerobot-train` 시작 직후(스텝 100~400 내) `FrameTimestampError`로 반복 크래시. 근본 원인 진단 후 수정 완료, 학습 재시작함.

**근본 원인**: 오늘까지 배치1·배치2 사고 복구 때 썼던 "메타데이터 수술" 패턴 — `meta/episodes/*.parquet`와 `info.json` 카운터만 고쳐서 문제 에피소드를 "삭제"하고, 실제 `data/*.parquet`/영상 바이트는 손대지 않고 그대로 둠(§ 위쪽 6.6/6.7 서술 그대로) — 이게 조용히 반쪽짜리였다. **학습 시 `LeRobotDataset`을 episode 필터 없이 전체 로드하면 그 "삭제된" 행들이 다시 이어붙는다** — 남은 행이 유효한 `episode_index` 값을 그대로 갖고 있어서 필터링되지 않기 때문. 그 결과 이후 모든 프레임의 전역 위치가 밀리면서, ACT의 액션 청크(chunk_size=100)가 에피소드 경계에서 클램핑될 때 엉뚱한 위치의 영상 프레임을 가리키게 됨.

- 감사 결과: `data/*.parquet` 실제 행 67,610개 vs `info.json` 선언 59,976개 — **유령 행 7,634개**, 파일 3곳(`file-001.parquet` 1049행/에피소드2, `file-002.parquet` 5914행/에피소드3~8, `file-004.parquet` 671행/에피소드19)에 분산돼 있었음. 전부 오늘 이전 크래시·중복촬영 복구 잔재
- 조치: 전체 데이터셋 백업(`C:\ACT_data\act_red_yellow_v1_prehostrowcleanup_backup_20260826_033306`) 후, 각 인증된 `meta/episodes`의 (episode_index → chunk/file) 매핑과 실제 파일 위치가 불일치하는 행만 골라 pyarrow로 제거. 이후 `data/*.parquet` 총 행수 정확히 59,976, `index` 컬럼이 0~59975 빈틈없는 연속열임을 확인
- 검증: 59,976 프레임 전체를 실제 학습과 동일한 조건(ACT delta_timestamps, `num_workers=4`, batch_size=8)으로 1회 전수 디코딩 — 오류 0건 (365초, 164 frames/s)
- **`tools/check_act_dataset.py`에 재발 방지 체크 추가** — `_validate_raw_data_row_count()`: `data/*.parquet` 실제 행수를 세어 `info.json`의 `total_frames`와 비교. 기존 검증은 `meta/episodes` 장부만 봐서 이 버그를 두 번이나(8/25 최초 완료 시, 8/26 재검증 시) 놓쳤었음
- 🔵 **교훈**: 앞으로 에피소드를 "삭제"하는 메타데이터 수술을 할 땐 `meta/episodes` 카운터 수정에서 끝내지 말고, 반드시 `data/*.parquet`에서도 해당 행을 물리적으로 제거할 것. 영상(`videos/*.mp4`)은 타임스탬프 범위로 조회되므로 이 문제와 무관 — 안 지워도 안전

학습 재시작: `train_log2.txt`, 3000+ 스텝 통과, ~6 step/s 안정적 진행 확인.

## 6.9 🔴 체크포인트 저장 시 심볼릭 링크 권한 크래시 (2026-08-26)

`train_log2.txt` — 스텝 20,000(첫 체크포인트 저장 시점)에서 크래시. 데이터 문제 아님 — 그 전까지 20,000스텝을 5.85 step/s로 완전히 안정적으로 통과함.

- 원인: `lerobot/utils/train_utils.py`의 `update_last_checkpoint()`가 매 체크포인트마다 `checkpoints/last` 심볼릭 링크를 무조건 생성 시도. Windows에서 심볼릭 링크 생성은 관리자 권한 또는 "개발자 모드" 활성화가 필요한데 이 노트북엔 없음 → `OSError: [WinError 1314] 클라이언트가 필요한 권한을 갖고 있지 않습니다`
- 체크포인트 본체(`checkpoints/020000/`)는 심볼릭 링크 시도 **이전에** 이미 정상적으로 다 저장돼 있었음 — 데이터 손실 없음
- `last` 심볼릭 링크는 순수 편의 기능. 실제 재개(`--resume=true`)는 `--config_path`로 특정 체크포인트의 `pretrained_model/train_config.json`을 직접 지정하는 방식이라 이 링크에 의존하지 않음 (`lerobot/configs/train.py` 확인)
- 조치: `tools/run_lerobot_train_patched.py` 신규 작성 — `update_last_checkpoint`를 몽키패치해서 심볼릭 링크 실패 시 경고만 남기고 계속 진행하도록 함. 설치된 lerobot 패키지 자체는 건드리지 않음
- 재개 명령: `--resume=true --config_path="...\checkpoints\020000\pretrained_model\train_config.json"` — 스텝 20,000부터 정확히 이어짐(진행바가 "80000"으로 표시 = 100000-20000), 옵티마이저·스케줄러 상태도 함께 복원 확인
- 🔵 **앞으로 `lerobot-train`을 돌릴 땐 항상 `tools/run_lerobot_train_patched.py`를 통해 실행할 것** (`tools/run_lerobot_module.py lerobot.scripts.lerobot_train` 대신) — 안 그러면 `save_freq`(기본 2만 스텝)마다 이 크래시가 반복됨

## 6.10 🔴 장시간 백그라운드 작업은 Claude 세션 백그라운드 작업(run_in_background)이 아니라 독립 프로세스로 띄울 것 (2026-08-26)

체크포인트 패치 후 재개한 학습이 **트레이스백 없이 두 번 연속(약 20~25분 만에) 조용히 종료**됐다(윈도우 이벤트 로그에도 앱 크래시·절전모드 진입 흔적 없음). Claude 세션의 백그라운드 작업 추적(`run_in_background`) 자체에 원인이 있는 것으로 추정하고, PowerShell `Start-Process`로 **세션과 무관한 완전 분리 프로세스**로 재실행하자 문제없이 **3시간 50분을 완주해 스텝 100,000에서 정상 종료**됐다.

- 🔵 **교훈**: 이 환경에서 30분을 넘기는 작업(특히 GPU 학습)은 Bash/PowerShell 도구의 `run_in_background`에 맡기지 말 것. 대신 `Start-Process -RedirectStandardOutput ... -RedirectStandardError ... -PassThru`로 띄워서 세션과 독립적인 프로세스로 돌릴 것. 진행 상황은 리다이렉트한 로그 파일을 계속 tail 하거나, PID로 `Get-Process`를 폴링해서 확인
- 최종 결과: `C:\ACT_data\training\hazardbot_red_yellow_act_v1\checkpoints\100000\pretrained_model\` — ACT 정책 완성 (52M 파라미터, `model.safetensors` 206.7MB). 체크포인트 20000/40000/60000/80000/100000 전부 정상 저장됨
- 다음 단계: 실물 로봇에 정책을 올려 롤아웃 테스트 (§ 위쪽에서 논의한 대로 — 학습 loss만으로는 데모 품질 보장 안 됨, 반드시 실제 시연으로 검증 필요)

## 6.7 🔴 수집 완료 (2026-08-26, 100/100)

`C:\ACT_data\act_red_yellow_v1` — **배치 1~10 전부 완료.**

- `check_act_dataset.py --mode full --require-complete`: 통과 (100 에피소드, 오류 0, 프레임 300개 샘플 디코딩 확인)
- `act_collection_plan.py verify --require-complete`: 통과
- 배치2에서 슬롯2 누락 + 슬롯10 중복 촬영 사고가 있었으나, 메타데이터 수술(중복분 삭제 + 슬롯3~10 인덱스 재매핑)로 복구 후 슬롯2만 별도 촬영해 마무리함 (§ 아래 배치1 기록 참고 — 배치1도 별도 사고·복구 이력 있음)
- 배치 3~10은 크래시·조작 실수 없이 각 1세션으로 깔끔하게 완료
- 다음 단계: §6 step 8 — 이 노트북에서 바로 `lerobot-train` 실행
- 🔵 **오늘 세션은 수집 완료 시점에서 종료.** 학습은 다음 세션에서 시작. §0 결론 요약 참고 후 바로 `lerobot-train` 명령으로 진입할 것 (재검증 불필요 — 위 검증 이미 통과)

## 6.6 배치 1 완료 (2026-08-25)

`C:\ACT_data\act_red_yellow_v1` — 10/10 에피소드, `check_act_dataset.py --mode full` 및 `act_collection_plan.py verify` 통과.

- 여러 차례 크래시·조작 실수로 재촬영을 거쳤다 (키보드 중복 입력, `Recording episode N`의 N이 0-indexed라는 것을 못 지켜 한 타임 늦게 동작한 것 등 — 다음 배치에서도 반복 주의)
- 🟡 **에피소드 5(슬롯6, 적색·뒤왼쪽)**: 뚜껑 닫기까지는 정상이나, 그 직후 불필요한 동작(다음 물체를 집으려는 듯한 움직임)이 몇 초 더 녹화됐다. 삭제하지 않고 그대로 두기로 함 — 학습 결과가 이상하면 이 에피소드부터 의심할 것
- 백업: `C:\ACT_data\act_red_yellow_v1_9ep_backup_2026-08-25`, `..._10ep_backup_2026-08-25b` — 과거 롤백 스냅샷, 필요 없어지면 삭제해도 됨

## 7. 절대 하지 말 것

| | 결과 |
|---|---|
| **연결 로그만 보고 팔로워 움직임 확인 생략** | 8/22와 동일하게 0에피소드로 끝난다 |
| **`teleop.ps1`을 그 컴퓨터 버전으로 덮어쓰기** | COM3/COM5·인덱스 0 등 이 노트북 검증값 소실 |
| **`--dataset.push_to_hub` 기본값 방치** | HuggingFace 업로드 |
| **`use_degrees` 플래그 누락** | 손목 폭주 위험 (`로봇암_구축기록.md` §5-7) |
| **강제 종료(Ctrl+C/작업관리자) 후 전원 안 끄기** | 토크 해제 미보장 — 8/22에 실제로 겪은 상황 |
| **빈/불완전 `output/` 폴더 자동 삭제·덮어쓰기** | 사람이 판단 후 처리 |
| **리더암에 12V** | 서보 6개 전멸 (₩178,315, 복구 불가) |
| **`-DatasetRoot`를 OneDrive `바탕 화면` 아래로 지정** | 청크 경계에서 ffmpeg concat 실패 (§6.5). 기본값(`C:\ACT_data`) 그대로 둘 것 |

---

## 막히면 볼 문서

| 찾는 것 | 위치 |
|---|---|
| 8/22 실패 상세 기록 | `ACT_수집_작업기록_2026-08-24.md` |
| 수집 설계 원본(정본) | 2026-08-09 Claude Artifact (링크는 위 문서 §3) |
| 텔레옵 막혔던 지점 7건 | `로봇암_구축기록.md` §5 |
| 캘리브레이션 원리·복원 | `calibration/README.md` |
| 수집 착수 전 전체 체크리스트 | `로봇암_카메라_구성정리.md` §11 |
| 별도컴퓨터 이관 절차(폐기된 접근) | `ACT_수집_다른컴퓨터_가이드.md` — 참고만, 지금은 안 씀 |
| 수집 프로토콜 ①~⑦ 원 출처 | `HazardBot_계획변경_및_개선안_v5.md` §6 |
